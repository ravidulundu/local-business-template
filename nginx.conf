server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    if ($host = www.ventoseacos.com.br) {
        return 301 https://ventoseacos.com.br$request_uri;
    }

    location = / {
        add_header Link '</index.md>; rel="alternate"; type="text/markdown", </sitemap.xml>; rel="sitemap", </robots.txt>; rel="robots"' always;
        if ($http_accept ~* "text/markdown") {
            rewrite ^ /index.md last;
        }
        try_files /index.html =404;
    }

    location = /index.md {
        types {
            text/markdown md;
        }
        default_type text/markdown;
        add_header x-markdown-tokens "approx=520" always;
        try_files /index.md =404;
    }

    location = /robots.txt {
        default_type text/plain;
        try_files /robots.txt =404;
    }

    location = /sitemap.xml {
        types {
            application/xml xml;
        }
        default_type application/xml;
        try_files /sitemap.xml =404;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(css|js)$ {
        expires -1;
        add_header Cache-Control "no-cache";
    }

    location ~* \.(jpg|jpeg|png|gif|ico|svg|webp|avif)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
